# .github/workflows/block-dotgithub.yml (this is what PRs trigger)
name: Safe Build PR

on:
  pull_request:
    branches:
      - "main"

permissions:
  contents: read

jobs:
  check-dotgithub:
    uses: thoughtworks/templify/.github/workflows/reusable/pr_build.yml@main
