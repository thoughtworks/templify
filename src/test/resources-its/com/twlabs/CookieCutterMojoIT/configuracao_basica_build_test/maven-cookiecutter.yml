steps:
  - kind: FileHandler
    metadata:
      type: xml
    spec:
      - files:
          - pom.xml
        placeholders:
          - match: /project/groupId
            replace: Cookiecutter.param.groupId
