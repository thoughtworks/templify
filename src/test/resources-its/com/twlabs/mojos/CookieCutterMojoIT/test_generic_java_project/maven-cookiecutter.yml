steps:
  - kind: XmlHandler
    apiVersion: v1
    spec:
      - files:
          - pom.xml
        placeholders:
          - match: /project/groupId
            replace: cookiecutter.groupId
          - match: /project/artifactId
            replace: cookiecutter.artifactId
  - kind: JavaHandler
    apiVersion: v1
    spec:
      - placeholders:
          - match: br.com.client.sfc.datalake.sfcdatatransferdatalake
            replace: cookiecutter.package
        baseDir: twlabs.com
