settings:
  placeholder:
    prefix: "${{"
    suffix: "}}"

mappings:
  - file: pom.xml
    placeholders:
      - query: com.api.new
        name: templify.TemPlate
      - query: DONt-CHange.HErE
        name: Need-tOBE--LoWer.Not.HerE

  - file: ./config/file.properties
    placeholders:
      - query: parametro1
        name: valor1
